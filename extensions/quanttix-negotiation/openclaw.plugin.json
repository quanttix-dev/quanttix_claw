{
  "id": "quanttix-negotiation",
  "name": "Quanttix Negotiation",
  "description": "Autonomous AR/AP negotiation agent over Telegram. Replaces the legacy Python handoff_server with a native OpenClaw extension: HTTP route, tools, inbound-claim hook, and Redis-backed cross-process binding store.",
  "enabledByDefault": false,
  "skills": ["./skills"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "mode": {
        "type": "string",
        "enum": ["disabled", "shadow", "active"],
        "default": "disabled",
        "description": "Rollout mode. 'disabled' is inert. 'shadow' creates negotiations server-side but never sends Telegram. 'active' is the full path."
      },
      "redisUrl": {
        "type": "string",
        "pattern": "^redis(s)?://",
        "default": "redis://127.0.0.1:6379",
        "description": "Redis connection string used for chat↔negotiation binding store."
      },
      "agentId": {
        "type": "string",
        "default": "quanttix-negotiation",
        "description": "OpenClaw agent id assigned by the inbound-claim hook when a chat has an active negotiation."
      },
      "bindingTtlSeconds": {
        "type": "number",
        "minimum": 60,
        "maximum": 604800,
        "default": 604800,
        "description": "TTL for chat↔negotiation binding entries (default 7 days, matching legacy handoff_server)."
      },
      "telegramBotToken": {
        "type": "string",
        "description": "Telegram Bot API token used to send the first message in 'active' mode. Falls back to env TELEGRAM_BOT_TOKEN."
      },
      "backend": {
        "type": "object",
        "additionalProperties": false,
        "description": "Quanttix Django backend connection. Service-account credentials are used by the plugin and the registered tools.",
        "properties": {
          "baseUrl": {
            "type": "string",
            "pattern": "^[Hh][Tt][Tt][Pp][Ss]?://",
            "description": "Quanttix Django backend base URL (e.g. https://test.quanttix.com.br)."
          },
          "email": {
            "type": "string",
            "description": "Service-account email (e.g. svc_quanttix_claw@quanttix.com)."
          },
          "password": {
            "type": "string",
            "description": "Service-account password. Stored alongside the Telegram bot token in openclaw.json."
          },
          "tenantId": {
            "type": "string",
            "description": "X-Tenant-Id header for multi-tenant routing."
          },
          "empresaId": {
            "type": "string",
            "description": "X-Empresa-Id header."
          },
          "filialId": {
            "type": "string",
            "description": "X-Filial-Id header."
          }
        }
      }
    }
  }
}
